/**
 * This tests the order of matching rules. `util` should be matched in the second group, even though
 * there is a similarly-named NodeJS module.
 */

import {} from 'fs';

import {} from 'util';

import {} from './relative';
